#import "./User.graphql"

type Mutation {
  signup(user: SignUpInput!): SignUpPayload!
  login(user: LoginInput!): LoginPayload!
  logout: Boolean!
  refreshToken(refreshToken: String!): RefreshTokenPayload!
  sessionDelete(id: ID!): Boolean!
}
